version: 0.2

phases:
  install:
    commands:
      - echo Installing...
  build:
    commands:
      - echo Build started on `date`
artifacts:
  files:
    - '**/*'
  base-directory: . # Build output will be uploaded from this folder
